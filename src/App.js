import React from "react";
import PropTypes from "prop-types";
import Router from "./router";

function App(props) {
  return (
    <div>
        <Router />
    </div>
  );
}

App.propTypes = {};

export default App;
